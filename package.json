{
  "name": "blw-kenya-zone-cloudflare",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "cf:typegen": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.0",
    "dotenv": "^16.4.1",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.1",
    "jsonwebtoken": "^9.0.0",
    "livekit-server-sdk": "^2.18.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.16.3"
  }
}
