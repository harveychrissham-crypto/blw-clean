import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { FiHash, FiChevronRight } from 'react-icons/fi';
import { fetchFeed, fetchTopics } from '../utils/feed';
function TopicCard({ topic }) { return <Link to={'/topics/' + encodeURIComponent(topic.slug)} className="flex items-center gap-3 rounded-2xl border border-white/[.07] bg-white/[.025] px-4 py-4 transition hover:bg-white/[.05]"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-[#1D9BF0]/10 text-[#42A5F5]"><FiHash /></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-bold text-white">{topic.name}</span><span className="mt-1 block text-xs text-white/35">{Number(topic.post_count || 0)} {Number(topic.post_count || 0) === 1 ? 'post' : 'posts'}</span></span><FiChevronRight className="text-white/25" /></Link>; }
export default function Topics() { const { slug: routeSlug } = useParams(); const slug=String(routeSlug||'').replace(/^#/,'').trim().toLowerCase(); const [topics,setTopics]=useState([]); const [posts,setPosts]=useState([]); const [loading,setLoading]=useState(true); useEffect(()=>{let active=true; Promise.all([fetchTopics(),slug?fetchFeed({limit:30,topic:slug}):Promise.resolve({posts:[]})]).then(([items,feed])=>{if(!active)return;setTopics(items);setPosts(feed.posts||[]);}).catch(()=>{if(active){setTopics([]);setPosts([]);}}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[slug]); return <main className="mx-auto min-h-screen w-full max-w-3xl px-4 pb-28 pt-6 sm:px-6"><header className="mb-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#42A5F5]">Discovery</p><h1 className="mt-2 text-2xl font-bold text-white">{slug?'#'+slug:'Topics'}</h1><p className="mt-2 text-sm text-white/45">{slug?'Conversations using this topic.':'Explore conversations by topic.'}</p></header>{loading?<div className="grid gap-3">{[1,2,3,4,5].map(i=><div key={i} className="h-20 animate-pulse rounded-2xl border border-white/[.05] bg-white/[.03]"/>)}</div>:slug?<div className="space-y-3">{posts.length?posts.map(post=><article key={post.id} className="rounded-2xl border border-white/[.07] bg-white/[.025] p-5"><Link to={post.authorEmail?'/u/'+encodeURIComponent(post.authorEmail):'/feed'} className="text-xs font-semibold text-white/55 hover:text-white">{post.author}</Link><h2 className="mt-2 text-base font-bold text-white">{post.title}</h2>{post.body&&<p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-white/70">{post.body}</p>}<div className="mt-4 text-xs text-white/30">{post.likeCount} likes · {post.commentCount} replies</div></article>):<div className="rounded-2xl border border-white/[.07] p-8 text-center text-sm text-white/40">No posts use this topic yet.</div>}</div>:<div className="grid gap-3 sm:grid-cols-2">{topics.length?topics.map(topic=><TopicCard key={topic.slug} topic={topic}/>):<div className="sm:col-span-2 rounded-2xl border border-white/[.07] p-8 text-center text-sm text-white/40">Topics will appear as people add hashtags to posts.</div>}</div>}</main>; }